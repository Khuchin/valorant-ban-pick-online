# v11.1 변경 내역

## 치명적 입력 버그

v11은 온라인 행동 요청을 보낸 상태에서 서버 스냅샷을 먼저 렌더링했습니다. 이때 `actionPending`이 아직 참이어서 현재 팀의 요원 카드가 모두 비활성 상태로 생성되고, 요청이 끝난 뒤에는 헤더만 다시 그려져 카드가 계속 잠긴 채 남을 수 있었습니다.

v11.1에서는:

- 카드의 소유 팀 판정과 네트워크 요청 중 입력 잠금을 분리했습니다.
- 요청 중에는 CSS로 잠깐 클릭만 차단합니다.
- 서버 상태가 먼저 오든 행동 응답이 먼저 오든 자신의 연속 차례가 정상적으로 활성화됩니다.
- 단계 불일치가 발생하면 장기 폴링을 즉시 재시작해 최신 상태를 받습니다.

## 성능

- 요원 카드 DOM을 캐시해 단계가 바뀌어도 29개 카드와 초상화를 매번 새로 만들지 않습니다.
- 상태가 실제로 바뀌지 않으면 드래프트 영역 자체를 다시 렌더링하지 않습니다.
- 맵 선택 목록과 맵 배경은 맵이 바뀔 때만 갱신합니다.
- 타이머는 0.5초마다 확인하되 표시값이 달라질 때만 DOM을 수정합니다.
- 재연결 과정에서 중복 동기화 루프가 생성되지 않도록 실행 세대 번호를 적용했습니다.
- 맵 배경 블러를 15px에서 6px로 줄이고 저사양 환경에서는 3px로 낮춥니다.
- `prefers-reduced-motion`과 저사양 장치에서 무거운 애니메이션을 자동 완화합니다.

## 공개 배포 표시

- 공식처럼 보일 수 있는 V 형태 마크를 중립적인 AD 마크로 변경했습니다.
- 화면 제목과 서버 패키지명을 Agent Draft Tool로 변경했습니다.
- 비공식·비제휴 고지와 개인정보 처리 범위를 서비스 정보 창에 추가했습니다.
